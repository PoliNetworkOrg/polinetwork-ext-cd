apiVersion: apps/v1
kind: Deployment
metadata:
  name: tutor-prod
  labels:
    app: app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tutor-prod
  template:
    metadata:
      labels:
        app: tutor-prod
    spec:
      containers:
        - name: app-dev
          image: zibas/poli-tutor-bot:d3738e614ee1668937dc291d5a0a73488ecac891
          imagePullPolicy: IfNotPresent
          volumeMounts:
            - name: config
              mountPath: /config
          resources:
            requests:
              cpu: "100m"
              memory: "300Mi"
            limits:
              cpu: "400m"
              memory: "1000Mi"
          ports:
            - containerPort: 5000
      volumes:
        - name: config
          secret:
            secretName: tutor-secrets
---
apiVersion: v1
kind: Service
metadata:
  name: tutor-prod-service
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 5000
  selector:
    app: tutor-prod
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: app-dev-ingress
  annotations:
    kubernetes.io/ingress.class: "nginx"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    nginx.ingress.kubernetes.io/use-regex: "true"
    nginx.ingress.kubernetes.io/rewrite-target: /tutorapp/$2
spec:
  tls:
    - hosts:
        - api.polinetwork.org
      secretName: app-dev-tls
  rules:
    - host: api.polinetwork.org
      http:
        paths:
          - path: /tutorapp(/|$)(.*)
            pathType: Prefix
            backend:
              service:
                name: app-dev-service
                port:
                  number: 8000
